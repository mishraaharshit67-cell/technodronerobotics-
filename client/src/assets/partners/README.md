Place partner logo images here (png/jpg/jpeg/svg).

File suggestions:
- partner1.png
- partner2.png

The `Partners` component auto-imports all images from this folder (Vite). After adding images, restart the dev server if needed.